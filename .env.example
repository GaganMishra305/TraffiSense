# Copy this file to ".env" and paste your real Mappls credentials.
# Get keys at https://apis.mappls.com/console/

# REST API key (used for geocoding / routing REST calls, server-side).
MAPPLS_REST_KEY=

# Map SDK / JavaScript key (used by the browser to render Mappls maps).
MAPPLS_MAP_SDK_KEY=
